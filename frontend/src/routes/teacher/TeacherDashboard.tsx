import PageHeader from "@/components/shared/PageHeader";

export default function TeacherDashboard() {
  return (
    <PageHeader
      title="Teacher Dashboard"
      description="A classroom/assignments home lands in a later session (Person B territory) — use Timetable, Attendance, Staffing, Early-Warning, and Syllabus in the meantime."
    />
  );
}
