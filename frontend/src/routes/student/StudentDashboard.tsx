import PageHeader from "@/components/shared/PageHeader";

export default function StudentDashboard() {
  return (
    <PageHeader
      title="Student Dashboard"
      description="A classroom/assignments home lands in a later session (Person B territory) — use Timetable in the meantime."
    />
  );
}
